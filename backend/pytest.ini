[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: needs the full stack and a live LLM (Ollama)
    db: needs a real PostgreSQL with pgvector (TEST_DATABASE_URL); no LLM
timeout = 120

